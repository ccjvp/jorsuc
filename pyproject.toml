[project]
name = "jorsuc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ccjvp", email = "casper@lumidb.com" }
]
requires-python = ">=3.9"
dependencies = [
    "torch>=2.8.0",
    "numpy>=1.26",
]

[project.scripts]
jorsuc = "jorsuc:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
